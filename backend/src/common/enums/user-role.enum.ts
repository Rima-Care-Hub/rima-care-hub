// the fields in the enum should be in lowercase eg admin = 'admin' not 'Admin = 'admin'
// using the uppercase in the enum fields will cause problems 

export enum UserRole {
  admin = 'admin',
  agency = 'agency',
  caregiver = 'caregiver',
}